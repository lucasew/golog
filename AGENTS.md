# Agents Instructions

- **Task Runner:** Use `mise` for all task execution.
- **Error Handling:** Use centralized error reporting via `internal/errors`.
- **Go Conventions:**
    - Use short, concise package names.
    - Avoid `default` package name.
    - Return interfaces or pointers from constructors.
    - Use pointer receivers for stateful structs.
